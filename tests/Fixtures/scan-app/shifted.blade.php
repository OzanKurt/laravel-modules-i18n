<div>
{{--
    A blade comment that spans several lines, so the compiled output is
    shorter than the source unless the scanner accounts for it.
--}}
<p>{{ __('shifted.after.comment') }}</p>
</div>
